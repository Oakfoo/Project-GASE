package modular.builder

// @GrabConfig(systemClassLoader=true)
// @Grab(group='com.h2database', module='h2', version="latest")

class GlobalController {

    // def prodUrl = Application.environments.production.dataSource.url
    // def devUrl = Application.environments.development.dataSource.url 
    // def current_environments = Application.getEnvironment()


    // def credentials = Application.DataSource
    // def sql = Sql.newInstance(
    //     credentials.driverClassName,
    //     credentials.username,
    //     credentials.password,
    //     devUrl
    //     );

    def index() {  }
}
